# Required in Codespaces. Copy this file to .env.local and set your Codespace name.
VITE_CODESPACE_NAME=your-codespace-name

# Without VITE_CODESPACE_NAME, the app falls back to http://localhost:8000/api.